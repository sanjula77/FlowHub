# Exclude everything that doesn't need to go into the Docker build context.
# This keeps builds fast and prevents secrets from leaking into the image.

node_modules/
dist/
.git/
.gitignore
.env
.env.*
!.env.example
coverage/
*.log
npm-debug.log*
.DS_Store
README.md
test/
.choreo/
