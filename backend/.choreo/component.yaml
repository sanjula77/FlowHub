schemaVersion: "1.2"
endpoints:
    - name: flowhub-api
      displayName: flowhub-api
      service:
        basePath: /
        port: 3001
      type: REST
      networkVisibilities:
        - Public   # Gives a public choreoapps.dev URL accessible from browsers
        - Project  # Also keeps internal project URL for service-to-service calls
