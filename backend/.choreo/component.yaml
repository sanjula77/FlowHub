schemaVersion: 1.1

# ── FlowHub Backend (NestJS) ──────────────────────────────────────────────────
endpoints:
  - name: flowhub-api
    type: REST
    networkVisibilities:
      - Project
    context: /
    service:
      # Must match the port your NestJS app listens on (process.env.PORT ?? 3001)
      port: 3001
